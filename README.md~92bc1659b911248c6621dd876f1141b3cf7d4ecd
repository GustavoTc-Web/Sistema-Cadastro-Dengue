# Sistema-Cadastro-Dengue
Projeto em Java com conexão MySQL feito para fins de estudo.


# Sistema de Cadastro - Projeto Java + MySQL

Este é um projeto simples desenvolvido em **Java**, com integração ao **banco de dados MySQL**, focado em praticar conceitos de programação, orientação a objetos e persistência de dados.

## 💻 Tecnologias utilizadas

- Java (console)
- MySQL
- JDBC

## 📌 Funcionalidades

- Conexão com banco de dados MySQL
- Inserção e consulta de dados
- Estrutura modular (separação entre lógica e conexão com o banco)

## 🚀 Como executar

> ⚠️ Este projeto **não é executável diretamente**, pois depende de um banco de dados local. O código é disponibilizado para fins de estudo.

Se desejar rodar o projeto:

1. Crie um banco de dados no MySQL com as tabelas necessárias.
2. Ajuste os dados de conexão no código (`usuário`, `senha`, `nome do banco`, etc.).
3. Compile e execute os arquivos `.java` usando uma IDE ou pelo terminal.

## 🛠️ Configuração do banco (exemplo)

```sql
CREATE DATABASE projeto_dengue;

USE projeto_dengue;

CREATE TABLE pacientes (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100),
    idade INT,
    regiao VARCHAR(50),
    sintomas TEXT
);
